import { EventEmitter, OnDestroy } from '@angular/core';
import { DataTable } from '../../';
export declare class DataTableRow<T> implements OnDestroy {
    dataTable: DataTable<T>;
    private _selected;
    expanded: boolean;
    item: T;
    index: number;
    selectedChange: EventEmitter<{}>;
    _this: this;
    constructor(dataTable: DataTable<T>);
    selected: boolean;
    readonly displayIndex: number;
    getTooltip(): string;
    ngOnDestroy(): void;
}
