import { DataTable } from '../../';
export declare class DataTableTitleComponent<T> {
    dataTable: DataTable<T>;
    columnSelectorOpen: boolean;
    constructor(dataTable: DataTable<T>);
    _closeSelector($event: any): void;
}
