import { OnInit, TemplateRef } from '@angular/core';
import { DataTableRow } from '../../';
import { CellCallback } from '../types';
export declare class DataTableColumn<T> implements OnInit {
    styleClassObject: {};
    header: string;
    sortable: boolean;
    resizable: boolean;
    property: string;
    styleClass: string;
    cellColors: CellCallback<T>;
    width: number | string;
    visible: boolean;
    cellTemplate: TemplateRef<any>;
    headerTemplate: TemplateRef<any>;
    getCellColor(row: DataTableRow<T>, index: number): string;
    ngOnInit(): void;
    private _initCellClass();
}
